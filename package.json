{
  "name": "roby-os",
  "version": "0.1.0",
  "private": true,
  "description": "Personal AI Operating System with agentic orchestration, specialist councils, memory and verification.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "desktop": "electron apps/desktop/main.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^44.4.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
